/**
 * A/B Test Manager Panel for Content Analytics Dashboard
 * Wrapper component for the A/B Test Manager
 */

import { ABTestManager } from '@/components/dashboard/ABTestManager';

export function ABTestManagerPanel() {
  return <ABTestManager />;
}